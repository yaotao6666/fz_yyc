# Checklist

- [x] 「实物订单」为独立菜单/路由（`/orders/goods`），请求固定 `category=1`，权限码 `order:goods`

- [x] 「服务订单」为独立菜单/路由（`/orders/service`），请求固定 `category=2`，权限码 `order:service`

- [x] 实物订单列含 工单状态/服务对象/服务人员/租赁到期，操作含 查看/派单/续租(仅租赁)

- [x] 服务订单列含 服务对象/工单状态/服务人员，**不含**租赁到期列与续租操作，操作含 查看/派单

- [x] 两类搜索条件各自独立（实物含订单类型=租赁/租赁到期维度，服务无租赁到期，含工单状态）

- [x] 两类订单均可派单（实物=配送，服务=上门服务），走 `order:dispatch`

- [x] `order:goods`、`order:service` 权限码三端一致（前端按钮、后端接口、sys\_menus）且已落库并授权 role\_id=1

- [x] 侧边栏菜单出现「实物订单」「服务订单」两个入口

- [x] 移除订单类型 5/6 后新旧枚举兼容，`npm run build` 通过

