# 财旭商贸 - 测试账号文档

## 目录

- [商户管理员账号](#商户管理员账号)
- [服务人员账号](#服务人员账号)
- [初始化数据说明](#初始化数据说明)
- [系统配置信息](#系统配置信息)

---

## 商户管理员账号

### 商户信息

| 字段 | 值 |
|------|-----|
| **商户ID** | 1 |
| **商户名称** | 美味餐厅 |
| **联系人** | 李四 |
| **联系电话** | 13900139000 |
| **联系邮箱** | lisi@example.com |
| **地址** | 北京市朝阳区建国路88号 |
| **经营品类** | 餐饮 |
| **状态** | 正常营业 |

### 商户管理员登录

| 字段 | 值 |
|------|-----|
| **用户名** | merchant |
| **密码** | merchant123 |
| **角色** | 商户管理员（owner） |
| **姓名** | 商户管理员 |
| **电话** | 13900139000 |
| **登录接口** | `/api/v1/auth/admin/login` |

### 商户配送设置

| 字段 | 值 |
|------|-----|
| **是否启用配送** | 是 |
| **基础配送费** | 5.00元 |
| **免费起送金额** | 50.00元 |
| **最大配送距离** | 10公里 |

### 商户商品分类

| 分类ID | 分类名称 | 排序 |
|--------|---------|------|
| 1 | 热销推荐 | 1 |
| 2 | 招牌菜 | 2 |
| 3 | 凉菜 | 3 |
| 4 | 主食 | 4 |
| 5 | 饮品 | 5 |

---

## 服务人员账号

### 服务人员登录

| 字段 | 值 |
|------|-----|
| **用户名** | staff |
| **密码** | staff123 |
| **姓名** | 测试服务人员 |
| **电话** | 13700137000 |
| **状态** | 正常 |
| **账号密码登录接口** | `/api/v1/service-staff/login` |
| **微信快捷登录接口** | `/api/v1/service-staff/wechat-login` |
| **注册申请接口** | `/api/v1/service-staff/register` |

---

## 初始化数据说明

当前仓库提供两种测试数据模式：

1. 最小初始化模式：仅保留登录和最小联调数据
2. 可选 mock 模式：在最小初始化基础上补充完整业务模拟数据

最小初始化模式下，单文件初始化脚本仅保留以下数据：

1. `PRD.md` 中约定的商户管理员账号 `merchant / merchant123`
2. `PRD.md` 中约定的服务人员账号 `staff / staff123`
3. 账号登录所需的最小商户、配送设置等基础数据

默认初始化脚本不再预置 C 端用户、订单、商品和行为演示数据。

说明：

1. 默认初始化阶段仅保留 `PRD.md` 中约定的商户管理员与服务人员登录账号。
2. 如需完整联调数据，可额外导入 `server/migrations/20260511120000_mock_seed.sql` 或执行 `./scripts/regression-test.sh init-mock`。
3. mock 模式会补充商户、商品、C 端用户、订单、行为事件、公告、打印机、服务人员等全链路数据。

---

## 系统配置信息

### 微信支付配置（系统级，测试用）

| 字段 | 值 |
|------|-----|
| **商户号** | 1234567890 |
| **API Key** | mock_api_key_12345 |
| **API v3 Key** | mock_api_v3_key_1234567890123456789012 |
| **证书序列号** | mock_cert_serial_no |

> 微信支付商户凭证作为系统级配置，通过环境变量注入。

### 其他初始化说明

默认单文件初始化脚本不预置平台活动、邀请奖励、商品、订单和 C 端用户数据，相关业务数据由联调、测试或运行过程自行产生。

可选 mock 模式会额外预置以下数据：

- 商户及对应支付配置、配送、员工数据
- 40+ 商品分类、80+ 商品、80+ 规格
- 30 个 C 端用户与 40+ 地址
- 120 条订单、240 条订单商品、20 条退款记录
- 120 条访问记录、300+ 条行为事件
- 服务人员数据
- 平台活动、公告、打印机、打印日志等运营数据

---

## 快速使用指南

### 1. 商户PC后台测试

**登录地址**：`web-admin` 本地开发地址 `/login`
**账号**：`merchant`
**密码**：`merchant123`

**测试功能**：
- 查看数据看板
- 商品与分类管理
- 订单管理与核销
- 服务人员管理
- 支付配置
- 公告管理
- 商户数据分析
- 健康档案管理（列表/详情/编辑，按关键字或评估等级筛选）
- 评估量表配置（新增/编辑/删除量表，草稿/启用切换）
- 评估记录查看（按用户关键字或量表筛选）
- 适配建议管理（按用户关键字或状态搜索、列表分页、详情抽屉、编辑弹窗（结论/推荐商品/状态/关联订单）、删除）
- 照护计划管理（按用户关键字/计划类型/状态搜索、列表分页（含照护次数）、详情抽屉（含照护记录表）、新增/编辑弹窗（居民远程搜索、护理项动态行、指派服务人员）、删除；存在照护记录的计划应拒绝删除）
- 随访任务管理（按用户关键字/状态/任务类型搜索、列表分页、手动登记、详情、执行弹窗（随访方式/内容/宣教文章多选/满意度）、管理员代执行；服务工单签退、租赁归还、健康评估完成后应自动生成随访任务，计划随访时间=完成+72小时，待认领任务可被服务人员认领）
- 生命体征监测（只读查询，按用户关键字/监测类型筛选、分页）
- 健康宣教管理（列表/新增编辑（标题/分类/封面/正文/定向慢病标签/状态/发布时间）/删除，仅已发布文章对 C 端可见）

### 2. 服务人员接单小程序测试

**登录方式**：服务人员接单小程序
**账号**：`staff`
**密码**：`staff123`

**测试功能**：
- 待接订单列表
- 接单操作
- 已接订单列表
- 订单详情
- 接单统计
- 客户健康档案查看（工单详情"客户健康档案"入口进入）
- 上门健康评估登记（为客户选择量表并提交，回显总分/等级/结论）
- 生成适配建议（客户档案页"适配建议"区块点"生成适配建议"，填写症状/适配结论并多选推荐商品+理由，提交后状态为草稿；未服务客户应返回 403）
- 我的照护计划（profile 页"我的照护计划"入口进入，仅展示指派给我的计划，查看详情与历史照护记录）
- 录入照护记录（计划详情或工单签退成功后的"录入照护记录"提示进入，护理项勾选/生命体征/照片/备注/下次随访建议提交；plan_id/order_id 至少其一且非本人负责范围应返回 403）
- 我的随访任务（profile 页"随访任务"入口进入，含待执行角标；列表按状态筛选，可见指派给我或待认领的任务）
- 执行/跳过随访（详情页选择随访方式（电话/上门/微信）、填写随访内容、多选宣教文章、评分满意度后提交，或跳过；待认领任务执行后自动认领；本人无权操作他人任务应返回 403）
- 客户生命体征（客户档案页"生命体征"区块查看历史记录并上门录入，非本人服务客户应返回 403）
- 随访自动生成验证：服务工单签退（租赁订单应生成租后回访、其他订单生成康复随访）、租赁归还、健康评估完成后，确认随访任务自动生成且计划随访时间=完成+72小时

### 3. C端小程序测试

**进入方式**：通过测试入口页面选择商户ID=1，或使用编译模式进入

**测试功能**：
- 浏览店铺
- 查看商品
- 加入购物车
- 下单购买
- 我的健康建档/编辑（首页"我的健康"入口进入）
- 自助健康评估（选择量表答题，提交后回显总分/等级/结论）
- 我的适配建议（首页"我的健康"入口网格进入，查看列表/详情、确认建议、对推荐商品"去选购"跳商品详情页复用下单链路）
- 我的照护计划（首页"我的健康"入口网格"照护计划"进入，查看我的计划列表/详情与历史照护记录；他人计划应不可见）
- 我的康复随访（首页"我的健康"入口网格"康复随访"进入，查看本人随访任务列表/详情与随访结果，完成/跳过状态回显）
- 健康宣教（首页"我的健康"入口网格"健康宣教"进入，列表按本人慢病标签匹配度排序并支持分类筛选，查看详情计浏览量；仅已发布文章可见）
- 我的体征记录（首页"我的健康"入口网格"体征记录"进入，查看本人记录列表并按类型筛选，自助录入体征（recorded_by=0））

> 健康模块数据权限校验点：服务人员仅能查看/评估/生成适配建议/录入体征给自己接单服务过的客户（无服务关系应返回 403）；web-admin 的健康服务菜单按 RBAC 权限码展示（`health:view` / `health:update` / `assessment:view` / `assessment:create/update/delete` / `fitting:view` / `fitting:update` / `care:view` / `care:create/update/delete` / `followup:view` / `followup:update` / `monitor:view` / `monitor:create` / `education:view` / `education:create`）；适配建议推荐商品在生成/编辑时校验存在且上架，状态流转为 草稿(0)→已确认(1)→已下单(2)；照护记录 `plan_id` / `order_id` 至少其一且均须属于当前服务人员负责范围（否则 403），存在照护记录的计划不可删除；随访任务在服务签退/租赁归还/评估完成后自动生成（计划随访时间=完成+72小时），待认领任务服务人员可执行并自动认领。

---

## 数据库初始化

### 方式1：直接导入初始化 SQL

```bash
cd server
docker exec -i fz_yyc_mysql mysql -uroot -proot123456 < migrations/20240101000000_full_init.sql
```

### 方式2：使用回归脚本

```bash
cd server
./scripts/regression-test.sh init
```

### 方式3：补充完整 mock 数据

```bash
cd server

# 在最小初始化基础上导入完整模拟数据
./scripts/regression-test.sh mock

# 或一步完成最小初始化 + 完整模拟数据
./scripts/regression-test.sh init-mock
```

### 方式4：使用 Docker

```bash
cd server

# 先确保容器已启动
docker compose up -d mysql redis

# 导入初始化脚本
docker exec -i fz_yyc_mysql mysql -uroot -proot123456 < migrations/20240101000000_full_init.sql
```

---

## 注意事项

1. **仅用于开发测试**：最小初始化和 mock 数据都仅供开发和测试环境使用，不要在生产环境使用
2. **密码安全**：生产环境请重置默认密码，使用强密码
3. **数据清理**：如需清空并重新初始化，请使用 `./scripts/regression-test.sh reset`
4. **API地址**：开发环境接口地址通常为 `http://localhost:8080`
