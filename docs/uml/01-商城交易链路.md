# 01 商城交易链路（C端用户）

> 面向 C 端用户的完整交易主流程。纵向节点串联，泳道区分"客户端小程序动作 / 服务端处理 / 微信支付"。
> 状态字段对齐 `orders.status`：1待支付 → 2已支付 → 3已完成 / 5退款中 / 4已取消 → 6已退款。
>
> 关联文档：[完整业务链路目录](../prd/完整业务链路目录.md) · [PRD-接口文档](../prd/PRD-接口文档.md)

```mermaid
flowchart TD
    %% ============ 泳道：客户端小程序 ============
    subgraph APP["客户端小程序"]
        direction TB
        A1[扫码进入商城首页] --> A2[自动登录\nPOST /auth/user/wechat-login]
        A2 --> A3[浏览轮播图/商品活动/分类宫格]
        A3 --> A4[商品详情\n选规格 · 租赁则选时长/押金]
        A4 --> A5{加购物车 or 立即购买}
        A5 -- 加购物车 --> A6[购物车结算]
        A5 -- 立即购买 buy_now=1 --> A7[确认订单页]
        A6 --> A7[确认订单页\n满减/押金明细/下单方式]
        A7 --> A8[提交订单 POST /store/orders]
        A8 --> A9{createOrder 返回 pay_params?}
        A9 -- 是 --> A10[唤起微信支付 JSAPI]
        A9 -- pay_amount=0 无 pay_params --> A11[展示支付成功/尾款提示]
        A10 --> A11
        A11 --> A12[查看订单 /user/orders]
        A12 --> A13{订单未支付?}
        A13 -- 是 --> A14[取消订单 POST cancel]
        A13 -- 否 --> A15[申请退款 POST refund]
        A15 --> A16[查看退款进度]
    end

    %% ============ 泳道：服务端处理 ============
    subgraph SRV["服务端"]
        direction TB
        S1[CreateOrder 校验库存/规格\n自动计算 order_type 与 biz_status] --> S2[生成微信下单参数 pay_params]
        S2 --> S3[保存订单 status=1 待支付\n表: orders / order_items]
        S3 --> S4[记录地址 users 关联 user_addresses]
        S5[校验签名并解密回调报文\nPOST /callback/wechatpay/pay] --> S6[更新 status=2 已支付\n写交易单号]
        S6 --> S7[推送 webhook 订单提醒 order_notify]
        S5 -. 退款回调\nPOST /callback/wechatpay/refund .-> S8[更新退款单 status=6 已退款\n表: refunds]
        S8 --> S9[向 PC 后台推送退款通知]
    end

    %% ============ 泳道：微信支付 ============
    subgraph PAY["微信支付"]
        direction TB
        P1[微信收银台] --> P2{用户支付}
        P2 -- 成功 --> P3[异步回调 notify_url\npartnerpayments/jsapi]
        P2 -- 失败/取消 --> P4[返回收银页重试或放弃]
    end

    %% ============ 跨泳道连线 ============
    A8 --> S1
    A10 --> P1
    P3 --> S5
    A12 --> S5
    A14 --> S4
    A15 --> S8
```

## 关键状态与说明

- **订单主状态 `orders.status`**：1待支付 → 2已支付 → 3已完成 / 5退款中（已发起退款）/ 4已取消 → 6已退款。
- **业务状态 `orders.biz_status`**：服务/租赁类订单下单时按商品类型自动写入（如待接单），普通商品为 0。
- **订单类型 `orders.order_type`**：1普通商品、2租赁商品、3即时服务、4预约服务、5上门服务、6到店服务。
- **押金 `orders.deposit_status`**：0无押金、1已收（租赁下单含押金）、2已退、3部分扣除。
- **支付身份**：服务端以 `app_mode`（sp_app/sub_app）选择对应 appid 发起 partnerpayments/jsapi 下单，回调走 `/api/v1/callback/wechatpay/pay`。
- **相关表**：`orders`、`order_items`、`refunds`、`user_addresses`、`users`。