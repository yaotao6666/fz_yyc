# 04 健康服务闭环（基层健康服务）

> "健康档案 → 人群评估 → 辅具适配 → 居家照护 → 持续随访 → 慢病宣教"连续闭环，体现三端（C端 / 服务人员端 / web-admin）数据流转。
> 状态常量对齐 `server/internal/utils/constants.go`。
>
> 关联文档：[健康闭环 Spec](../../.trae/specs/build-health-service-closed-loop/spec.md) · [PRD-功能说明](../prd/PRD-功能说明.md)

```mermaid
flowchart TD
    %% ============ 泳道：C端小程序 ============
    subgraph CE["C端小程序"]
        direction TB
        CE1[我的健康入口] --> CE2[自助评估\nassessor_type=1]
        CE2 --> CE2b[查看评估结论]
        CE1 --> CE3[查看适配建议]
        CE3 --> CE3b[一键加购/下单\n复用下单链路 order_type 自动]
        CE1 --> CE4[查看照护计划与记录]
        CE1 --> CE5[我的体征记录/自助录入\nmonitoring]
        CE1 --> CE6[健康宣教列表\n按本人慢病标签匹配度排序]
        CE1 --> CE7[查看康复随访任务]
    end

    %% ============ 泳道：服务人员端 ============
    subgraph STAFF["服务人员端"]
        direction TB
        SF1[客户健康档案查看\n数据权限: 仅服务过的客户] --> SF2[上门评估\nassessor_type=2 · StaffCreateAssessment]
        SF2 --> SF3[生成适配建议\nfitting_recommendations]
        SF1 --> SF4[照护计划执行\ncare_plans 指派可见]
        SF4 --> SF5[录入上门照护记录\ncare_visits: 护理项/体征/照片]
        SF1 --> SF6[体征监测录入\nrecorded_by=服务人员]
        SF1 --> SF7[待办随访执行\nfollow-up-tasks complete/skip]
    end

    %% ============ 泳道：web-admin ============
    subgraph ADM["web-admin"]
        direction TB
        WD1[健康档案管理] --> WD2[评估量表配置\nhealth_assessment_forms\n草稿0/启用1]
        WD2 --> WD3[评估记录查看]
        WD1 --> WD4[照护计划创建/指派\n可生成预约服务订单进待接单池]
        WD1 --> WD5[随访任务查看/手动创建]
        WD1 --> WD6[生命体征监测记录]
        WD7[健康宣教文章创建/发布\nstatus 草稿0/发布1 · 定向慢病标签] 
    end

    %% ============ 泳道：服务端数据底座 ============
    subgraph SRV["服务端 · 数据底座"]
        direction TB
        SR1[(health_records\n居民健康档案\n每用户至多一条)]
        SR2[(health_assessments\n评估记录)]
        SR3[(fitting_recommendations\n适配建议)]
        SR4[(care_plans / care_visits\n照护计划与上门记录)]
        SR5[(follow_up_tasks\n自动随访任务)]
        SR6[(health_monitoring\n体征监测)]
        SR7[(health_education_articles\n健康宣教)]
        SR8[自动生成随访任务\n触发: 服务完成/租赁归还/评估完成\nplan_follow_time=now+72h]
    end

    %% ============ 跨泳道连线（闭环） ============
    SR1 --> CE1
    SR1 --> SF1
    SR1 --> WD1
    CE2 --> SR2 --> SR3
    SF2 --> SR2 --> SR3
    CE2 -.评估完成.-> SR8
    SF2 -.评估完成.-> SR8
    SR3 --> CE3
    SF3 --> SR3
    WD4 --> SR4 --> SF4
    SF5 --> SR4 --> CE4
    WD6 --> SR6
    SF6 --> SR6
    CE5 --> SR6
    WD5 --> SR5
    SR8 --> SR5 --> SF7
    SF7 --> SR5
    SR5 --> CE7
    WD7 --> SR7 --> CE6
    SR1 -.评估等级回写 assessment_level.-> SR1
```

## 关键状态与说明

- **居民健康档案 `health_records.status`**：0未建档 / 1正常 / 2已归档；`chronic_tags`（慢病标签数组）支撑宣教定向与随访计划。
- **评估 `health_assessments.assessor_type`**：1自助（C端）/ 2服务人员；表单 `health_assessment_forms.status`：0草稿/1启用（草稿对端不可见）。
- **评估结论回写**：评估完成后将 `total_score/level/conclusion/suggestions` 回写 `health_records.assessment_level` 与 suggestions。
- **辅具适配 `fitting_recommendations.status`**：0草稿 / 1已确认 / 2已下单，可选择关联商品，C端一键加购复用下单链路。
- **照护 `care_plans.status`**：0草稿 / 1执行中 / 2暂停 / 3完成；`care_visits` 记录护理项/生命体征/照片。
- **随访 `follow_up_tasks`**：触发节点为服务完成（source=1）、租赁归还（source=2）、评估完成（source=3）、手动（source=4）；状态 0待执行/1完成/2跳过；计划随访时间 `now + 72h`。
- **数据权限隔离**：健康档案/评估/监测仅本人（C端）、服务过的客户（服务人员）、管理员（web-admin）可见。
- **相关表**：健康九表 + `orders`（适配/照护关联） + `users`（档案主体）。