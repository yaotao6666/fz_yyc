# 02 服务人员工单链路（服务人员端）

> 服务人员从登录到接单、履约、签退并触发随访的完整流程。
> 业务状态字段对齐 `orders.biz_status`：0无 → 1待接单 → 2已接单 → 3服务中 → 5已完成 / 6已取消。
>
> 关联文档：[PRD-功能说明](../prd/PRD-功能说明.md) · [PRD-接口文档](../prd/PRD-接口文档.md)

```mermaid
flowchart TD
    %% ============ 泳道：注册/登录 ============
    subgraph AUTH["注册 · 登录"]
        direction TB
        A1[PC 后台添加 或 小程序注册申请] --> A2[后台审核通过]
        A2 --> A3[登录 POST /service-staff/login\n或 wechat-login] --> A4[进入工作台账]
    end

    %% ============ 泳道：接单 ============
    subgraph ACC["接单"]
        direction TB
        B1[待接订单池\nstatus=2已支付 + biz_status=1待接单\n+ assigned_staff_id IS NULL\nGET /orders/pending]
        B1 --> B2[查看订单详情] --> B3{是否接单}
        B3 -- 是 --> B4[接单 POST /orders/:id/accept\n原子更新 assigned_staff_id + biz_status=2 已接单]
        B3 -- 否 --> B1
    end

    %% ============ 泳道：履约 ============
    subgraph EXE["服务履约"]
        direction TB
        C1[签到 POST /orders/:id/check-in\nbiz_status=3 服务中 · actual_started_at]
        C1 --> C2[上门/到店提供服务\n可查看客户健康档案/录入照护/监测]
        C2 --> C3[签退 POST /orders/:id/check-out\nbiz_status=5 已完成 · actual_ended_at]
        C3 --> C4{订单类型判定}
        C4 -- 租赁订单 order_type=2 --> C5[更新 rental_returned_at\n生成 租后回访随访任务\nfollow_up_tasks source_type=2]
        C4 -- 其他服务 --> C6[生成 康复随访任务\nfollow_up_tasks source_type=1]
    end

    %% ============ 泳道：待办/统计 ============
    subgraph TODO["待办 · 统计"]
        direction TB
        D1[待办列表 GET /todo<br/>待出发+服务中 biz_status 2 或 3]
        D1 --> D2[今日接单/完成统计]
        D1 --> D3[执行随访任务\nfollow-up-tasks/:id/complete 或 skip]
        D1 --> D4[录入体征监测 monitoring]
    end

    %% ============ 连线 ============
    A4 --> B1
    B4 --> C1
    C5 --> D1
    C6 --> D1
    D3 --> B1
```

## 关键状态与说明

- **业务状态 `orders.biz_status` 流转**：待接单(1) → 已接单(2) → 服务中(3) → 已完成(5)；取消为 6。
- **接单原子性**：仅在 `status=2 且 biz_status=1 且 assigned_staff_id IS NULL` 时可通过 UPDATE 抢占接单，避免并发重复接单。
- **签退触发随访**（见 [workorder.go](../../server/internal/handlers/service_staff/workorder.go) 与 [auto.go](../../server/internal/services/followup/auto.go)）：
  - 租赁订单（`order_type=2`）→ 租后回访随访（source_type=2）；
  - 普通服务 → 康复随访（source_type=1）；
  - 计划随访时间 = 当前时间 + 72 小时，失败仅记录不影响签退主流程。
- **服务人员职责边界**：不执行订单核销与退款，仅负责接单、履约、签退。
- **相关表**：`orders`、`order_items`、`service_staffs`、`follow_up_tasks`、`care_visits`、`health_monitoring`。