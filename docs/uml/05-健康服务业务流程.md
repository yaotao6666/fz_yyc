# 05 健康服务业务流程（三端实战时序）

> 从"使用者怎么操作"视角梳理健康服务完整业务流程，跨 C端 / 服务人员端 / web-admin 三端，标注关键接口。
> 该图聚焦『如何使用』的动作与接口调用顺序；数据结构与状态常量见 [04-健康服务闭环](./04-健康服务闭环.md)。
>
> 关联文档：[PRD-接口文档](../prd/PRD-接口文档.md) · [PRD-功能说明](../prd/PRD-功能说明.md)

```mermaid
flowchart TD
    %% ================= 泳道：web-admin 管理员 =================
    subgraph ADM["web-admin 管理员"]
        direction TB
        W1["登录 RBAC 鉴权"] --> W2["配置评估量表<br/>POST /merchant/assessment-forms 草稿0/启用1"]
        W2 --> W3["配置健康宣教<br/>POST /merchant/health-education 草稿0/发布1"]
        W3 --> W4["创建照护计划并指派<br/>POST /merchant/care-plans status→1 执行中"]
        W4 --> W5["查看评估/随访/监测记录"]
    end

    %% ================= 泳道：服务人员端 =================
    subgraph STAFF["服务人员端"]
        direction TB
        S1["登录 POST /service-staff/login"] --> S2["接单/签到/签退（工单链路）"]
        S2 --> S3["查看已服务客户健康档案<br/>GET /residents/:user_id/health-record<br/>数据权限: 仅服务过的客户"]
        S3 --> S4["上门评估（若需要）<br/>POST /residents/:user_id/assessments<br/>assessor_type=2"]
        S4 --> S5["生成适配建议<br/>POST /residents/:user_id/fitting-recommendations"]
        S2 --> S6["按计划上门照护<br/>POST /care-visits 护理项/体征/照片"]
        S6 --> S7["录入体征监测<br/>POST /residents/:user_id/monitoring"]
        S2 --> S8["执行随访任务<br/>POST /follow-up-tasks/:id/complete 或 skip"]
    end

    %% ================= 泳道：C端用户 =================
    subgraph USER["C端用户"]
        direction TB
        U1["我的健康入口"] --> U2["查看/编辑健康档案<br/>GET/POST /user/health-record<br/>每用户至多一条"]
        U1 --> U3["自助评估<br/>GET /user/assessment-forms + POST /user/assessments<br/>assessor_type=1"]
        U1 --> U4["查看适配建议<br/>GET /user/fitting-recommendations"]
        U4 --> U5["一键加购/下单（复用交易链路）"]
        U1 --> U6["查看照护计划/记录<br/>GET /user/care-plans"]
        U1 --> U7["查看康复随访<br/>GET /user/follow-ups"]
        U1 --> U8["按慢病标签匹配健康宣教<br/>GET /user/health-education"]
        U1 --> U9["体征记录/自助录入<br/>GET+POST /user/monitoring"]
    end

    %% ================= 泳道：服务端数据与触发 =================
    subgraph SRV["服务端 · 自动触发与回写"]
        direction TB
        G1["评估完成→回写档案 assessment_level <br/>+ 自动生成评估回访随访任务"]
        G2["服务签退 / 租赁归还→<br/>自动生成随访任务 plan_follow_time=now+72h"]
        G3["宣教按用户 chronic_tags 定向排序推送"]
        G4["评估/照护/监测结果沉淀到九张健康表"]
    end

    %% ================= 跨泳道连线（业务闭环） =================
    W4 -->|指派| S6
    S5 -->|同步到端| U4
    W5 --> G4
    S4 --> G1 --> G4
    S6 --> G4
    S7 --> G4
    S8 --> G2
    S2 -. 服务/归还完成 -.-> G2
    G2 --> S8
    G1 --> S8
    U8 --> G3
    U3 --> G1
    U5 -.->|进入商城下单→工单池| S2
```

## 关键流转说明（使用者视角）

1. **web-admin 先配置**：量表（启用后对端可见）、宣教（发布后 C 端可见）、照护计划（指派服务人员）。
2. **服务人员上门**：接单 → 查看客户档案（仅服务过的客户）→ 依需评估/照护/监测/随访。
3. **C 端自助**：查看档案、自测评估、按适配建议加购下单、查看照护与随访、定向宣教。
4. **自动闭环**：评估完成回写档案等级并生成随访；服务/租赁归还签退自动生成随访任务（now+72h）；宣教按 `chronic_tags` 定向。
5. 服务人员在 C 端用户下单后进入工单链路（见 [02-服务人员工单链路](./02-服务人员工单链路.md)）。

## 数据权限隔离

- 服务人员仅能访问自己服务过的客户健康数据（`assigned_staff_id` 数据域校验）。
- C 端用户仅能访问本人档案/评估/监测/随访/适配。
- web-admin 管理员可全量查看与管理。