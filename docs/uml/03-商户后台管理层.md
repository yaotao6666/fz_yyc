# 03 商户后台管理层（web-admin）

> 单商户 PC 后台经营管理与系统管理主流程。RBAC 三层权限闭环（接口 middleware.RBAC / 页面路由守卫 / 按钮 v-permission）。
>
> 关联文档：[PRD-功能说明](../prd/PRD-功能说明.md) · [PRD-接口文档](../prd/PRD-接口文档.md)

```mermaid
flowchart TD
    %% ============ 泳道：登录与权限 ============
    subgraph RBAC["登录 · 权限"]
        direction TB
        R1["管理员登录 POST /auth/admin/login"] --> R2["签发 token + 返回菜单树 + 权限码"]
        R2 --> R3["RBAC 分层鉴权<br/>接口 middleware.RBAC · 页面守卫 · 按钮 v-permission<br/>RBACAny 任一权限通过"]
        R3 --> R4["菜单/角色/部门/员工管理<br/>sys_menus | sys_roles | sys_role_menus<br/>sys_departments | merchant_staff_roles<br/>RBAC 60s 缓存"]
    end

    %% ============ 泳道：门店经营 ============
    subgraph OPS["门店经营"]
        direction TB
        O1["店铺资料维护 profile"] --> O2["分类管理 三级分类 · 分类树 BuildTree"]
        O2 --> O3["商品管理 一口价/租赁 · 规格库存 · 上/下架"]
        O3 --> O4["服务人员管理 后台添加 | 注册申请审核 | 启停"]
    end

    %% ============ 泳道：订单履约与数据 ============
    subgraph ORDER["订单与数据"]
        direction TB
        F1["接收订单提醒 order_notify"] --> F2["查看订单列表/详情"]
        F2 --> F3{处理动作}
        F3 -- 核销 --> F4["核销完成 orders.status→3<br/>biz_status→5 已完成"]
        F3 -- 退款 --> F5["退款处理 orders→5 退款中 / 6 已退款<br/>调微信退款 · 先建 refunds 记录"]
        F3 -- 租赁归还 --> F6["归还退押金 deposit_status→2 已退 / 3 部分扣除"]
        F2 -.-> F7["经营数据分析 analytics<br/>销售额/趋势/排行/顾客/库存预警"]
    end

    %% ============ 泳道：健康服务管理 ============
    subgraph HEALTH["健康服务管理"]
        direction TB
        H1["健康服务菜单 档案/量表/评估/适配/照护/随访/监测/宣教"]
        H1 --> H2["档案与评估管理"]
        H1 --> H3["照护计划与上门记录"]
        H1 --> H4["随访任务与健康宣教"]
    end

    %% ============ 连线 ============
    R2 --> O1
    R3 --> O1
    R3 --> H1
    %% 审核通过后服务人员进入工单链路(见 02)，服务单核销后进入待接单池(见 02)
    O4 -.-> |审核通过| X1["进入服务人员工单链路"]
    F4 -.-> |服务/租赁订单| X2["进入待接单池"]
```

## 关键状态与说明

- **RBAC 三层权限闭环**：
  - 接口级：`middleware.RBAC("order:view")` 注入 `/merchant/*` 路由，无权限返回 403；
  - 页面级：vue-router 守卫按菜单/权限重定向；
  - 按钮级：`v-permission` 指令移除无权限按钮。
- **订单处理状态**（`orders.status`）：核销 2→3 已完成；退款 2→5 退款中→6 已退款；取消 4。
- **退款时序**：先创建 `refunds` 记录，再调用微信退款接口（回调 `/callback/wechatpay/refund`），保证落库与对账一致。
- **租赁归还退押金**：填写扣除金额与验机备注 `rental_return_remark`，更新 `deposit_status` 与退还/扣除金额。
- **相关表**：`orders`、`orders_items`、`refunds`、`categories`、`products`、`service_staffs`、RBAC 五表、健康九表。