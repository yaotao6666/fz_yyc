* [x] 商家商品详情接口在“商品不存在”场景返回 `404`

* [x] 商家商品详情接口在“关系加载/内部实现异常”场景返回 `500`

* [x] 商品创建后详情回读失败时返回 `500`

* [x] 商品更新后详情回读失败时返回 `500`

* [x] 接口不再把内部错误统一包装成“商品不存在”

* [x] 前端编辑页可基于准确错误语义进行展示与排查

* [x] 前端请求层在非 `200` 响应时透传后端错误码与错误信息，而不是统一提示 `请求失败(status)`
