## 总结

目标：基于 [接口与页面对接差异报告.md](file:///e:/other/fz_yyc/.trae/documents/接口与页面对接差异报告.md) 逐步修复，实现 **PRD ↔ 后端 ↔ 小程序** 全量对齐，并**启用 SP 端**（页面可进入、接口可登录、鉴权可用）。同时按要求**移除 `/api/v1/admin/*` 兼容路由**，并同步更新 PRD 文档。

范围决策（已确认）：
- 修复范围：全量对齐 + 启用 SP
- 数据库变更：允许新增迁移（云打印相关字段对齐 PRD）
- 文档同步：接口/表结构变更同步更新 [PRD.md](file:///e:/other/fz_yyc/PRD.md)
- 移除兼容：不再需要 `/api/v1/admin/*` 兼容路由，删除相关后端代码与 PRD 相关描述

## 当前状态分析（基于仓库事实）

### 1) 路由入口与现有问题
- 后端路由集中在 [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go#L64-L280) `setupRoutes()`。
- 关键缺口/不一致（见差异报告）：
  - 缺少：`POST /api/v1/merchant/register`
  - 缺少：`GET /api/v1/merchant/print-logs`
  - PRD 口径与实现不一致：`POST /api/v1/user/auth/wechat-login` vs `/api/v1/auth/user/wechat-login`
  - SP 登录结构性问题：`/api/v1/sp/auth/login` 被 `JWTAuth()` 保护（[main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go#L229-L235)、[auth.go](file:///e:/other/fz_yyc/server/internal/middleware/auth.go#L55-L89)）
  - PRD 定义但后端未注册：用户地址、商家订阅、云打印机 CRUD、商品规格独立接口、`/api/v1/upload/callback`、`/api/v1/callback/wechat`、`/api/v1/user/merchant/status`
- 目前存在 `/api/v1/admin/*` 兼容路由（[main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go#L125-L161)），本次需要删除。

### 2) 数据模型现状（可复用）
- 数据库迁移已包含：`product_specs`、`user_addresses`、`cloud_printers`、`print_logs`（[full_init.sql](file:///e:/other/fz_yyc/server/migrations/20240101000000_full_init.sql#L255-L612)）。
- Go 模型已包含：`ProductSpec`、`UserAddress`、`CloudPrinter`、`PrintLog`（[models.go](file:///e:/other/fz_yyc/server/internal/models/models.go#L285-L685)）。
- 但部分 PRD 字段（`auto_print/is_default/print_count`）目前 **DB 表与模型缺失**（PRD 见 [PRD.md](file:///e:/other/fz_yyc/PRD.md#L2856-L2881)，DB 表定义见 [full_init.sql](file:///e:/other/fz_yyc/server/migrations/20240101000000_full_init.sql#L578-L612)）。

### 3) 小程序 SP 端可用性现状
- 页面源码存在 `miniprogram/src/pages/sp/**`，但未注册到 [pages.json](file:///e:/other/fz_yyc/miniprogram/src/pages.json#L1-L220)。
- `spStore` 使用 `sp_token`（[sp.ts](file:///e:/other/fz_yyc/miniprogram/src/stores/sp.ts#L11-L57)），但请求封装默认只读 `token`（[request.ts](file:///e:/other/fz_yyc/miniprogram/src/utils/request.ts#L45-L47)），导致 SP 接口即使可登录也无法带对 token。

## 拟议改动（按“阻塞 → 结构 → 全量对齐”顺序）

> 说明：执行阶段会按以下顺序逐项落地，每一步都包含可验证的接口/页面验收点，避免大改导致回归面不可控。

### A. 后端路由清理：移除 `/api/v1/admin/*` 兼容路由
改动文件：
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
改动内容：
- 删除 `adminGroup := v1.Group("/admin") ...` 及其所有路由注册（当前仅为“向后兼容重定向到 /sp”）。
- PRD 中删除/修改“兼容路由保留/重定向”相关描述与对接表条目，确保文档不再出现 `/api/v1/admin/*` 作为对外口径。
验收点：
- 服务端启动后不再暴露 `/api/v1/admin/*`。
- PRD 对接表不再包含 `/api/v1/admin/*`。

### B. 后端：补齐“前端已调用但后端缺失”的阻塞接口
#### B1. `POST /api/v1/merchant/register`
改动文件：
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- [merchant/handler.go](file:///e:/other/fz_yyc/server/internal/handlers/merchant/handler.go)
- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
实现要点：
- 路由放在 `v1` 下（不走 `merchantGroup.Use(JWTAuth())`），与 PRD “入驻申请”一致。
- 请求体按前端类型 [MerchantRegisterRequest](file:///e:/other/fz_yyc/miniprogram/src/types/index.ts#L185-L215) 与 PRD 章节 [PRD.md:L2756-L2805](file:///e:/other/fz_yyc/PRD.md#L2756-L2805) 对齐。
- 服务器侧落库策略（最小可用且可扩展）：
  - 新建 `merchants` 记录（`service_provider_id` 先取默认 1；`contact_phone/contact_name/contact_email/address/business_category/name` 写入；`applyment_status/audit_status` 初始化为“待提交/待审核”的状态码）。
  - 新建 `merchant_applications` 记录（把 license/bank/store/contact 信息写入对应 JSON 字段）。
  - 响应返回 `{ merchant_id, application_id, status }`（status 先返回 `draft` 或 `submitted`，与 PRD 约定一致）。
验收点：
- 小程序 [register.vue](file:///e:/other/fz_yyc/miniprogram/src/pages/auth/register.vue#L394-L426) 能提交成功且返回 application_id。

#### B2. `GET /api/v1/merchant/print-logs`
改动文件：
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- 新增或扩展 `merchant` handler（建议在 `merchant` 包新增独立文件以保持可读性）
- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
实现要点：
- 路由放在 `merchantGroup`（JWTAuth）。
- 查询 `print_logs`，支持 `page/page_size/start_date/end_date`（前端封装见 [api/index.ts:L753-L755](file:///e:/other/fz_yyc/miniprogram/src/api/index.ts#L753-L755)）。
- 输出结构与 PRD 对齐（打印时间可由 `created_at` 映射；避免返回打印机 `api_key` 等敏感字段）。
验收点：
- 小程序调用 `getPrintLogs()` 返回 200 且结构符合 PRD。

### C. 后端：修正/补齐 PRD 路径口径不一致（兼容联调）
#### C1. C 端微信登录：补齐 `POST /api/v1/user/auth/wechat-login`
改动文件：
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
实现要点：
- 在保留现有 `POST /api/v1/auth/user/wechat-login` 的同时，新增一条 PRD 口径路由 `POST /api/v1/user/auth/wechat-login` 指向同一 handler（[main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go#L86-L93)）。
- PRD 保持 `/api/v1/user/auth/wechat-login` 为主口径，并在文档中标注兼容路由（若需要）。
验收点：
- 两条路径均可登录成功。

#### C2. 微信支付回调：补齐 `POST /api/v1/callback/wechat`
改动文件：
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- 复用现有回调 handler（当前为 `POST /api/v1/notify/payment`）
- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
实现要点：
- 新增 `POST /api/v1/callback/wechat`，内部复用 `admin.PaymentNotify`（保持原 `/notify/payment` 不动，避免线上已有回调地址失效）。
验收点：
- 两条回调路径均可返回正确响应（按现有支付回调逻辑）。

### D. 启用 SP 端：后端鉴权结构 + 小程序路由/鉴权
#### D1. 后端：SP 登录改为“登录不鉴权，其余鉴权”
改动文件：
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- （如需）SP handler 内对用户类型校验（可复用现有 token user_type=admin/sp）
实现要点：
- 将 `POST /api/v1/sp/auth/login` 挪到不经过 `JWTAuth()` 的分组。
- `POST /api/v1/sp/auth/logout` 与其余 `/api/v1/sp/*` 保持 JWTAuth。
验收点：
- 未带 Authorization 能调用登录；登录后 token 可访问 `/api/v1/sp/dashboard` 等。

#### D2. 小程序：注册 SP 页面路由并正确使用 `sp_token`
改动文件：
- [pages.json](file:///e:/other/fz_yyc/miniprogram/src/pages.json)
- [request.ts](file:///e:/other/fz_yyc/miniprogram/src/utils/request.ts)
实现要点：
- 在 pages.json 增加 `/pages/sp/*` 相关路由（登录、首页、商家审核、公告等），并在 condition 增加 SP 登录入口方便调试。
- request.ts 根据 URL 前缀选择 token：
  - `/api/v1/sp/` 使用 `sp_token`
  - 其他继续使用 `token`
验收点：
- 能进入 `/pages/sp/login` 页面并登录成功；登录后能请求 `/api/v1/sp/dashboard`。

### E. PRD 缺失接口全量补齐（按 PRD 定义实现）
#### E1. 用户地址 CRUD：`/api/v1/user/addresses*`
改动文件：
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- user handler（新增或扩展 [user/handler.go](file:///e:/other/fz_yyc/server/internal/handlers/user/handler.go)）
- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
实现要点：
- 放在 `userGroup.Use(JWTAuth())` 下（与 PRD 一致）。
- 实现列表、新增、更新、删除；处理默认地址唯一性（设置默认时需清空其它记录）。
验收点：
- CRUD 均返回 PRD 结构，且默认地址逻辑正确。

#### E2. 商家订阅配置：`GET/PUT /api/v1/merchant/subscriptions`
改动文件：
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- merchant handler
- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
实现要点（在现有数据结构上实现 PRD 结构）：
- 由于 DB 无独立 subscriptions 表，本次以 `merchant_staffs.openid/notify_enabled/browse_notify_enabled` 为基础：
  - GET：返回 PRD 的 `order_new/order_paid/order_refund` 三项，`enabled` 使用 `notify_enabled`，`push_openid` 使用当前员工 openid。
  - PUT：若三项 enabled 不一致，返回参数错误；否则统一写回 `notify_enabled`。`push_openid` 写回当前员工 openid（如需）。
验收点：
- 接口存在且结构与 PRD 对齐；与现有“声音提醒开关”设置不冲突。

#### E3. 云打印机 & 打印记录：`/api/v1/merchant/printers*` + `print-logs`
改动文件：
- 新增迁移：为 `cloud_printers` 增加 `auto_print/is_default/print_count` 字段（不破坏已有数据）
- [models.go](file:///e:/other/fz_yyc/server/internal/models/models.go)（补齐字段并保持 json 输出对齐 PRD）
- merchant handler：实现 printers 列表/新增/更新/删除/测试 + print-logs 列表
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
实现要点：
- printers CRUD 均需 JWTAuth，且按 merchant_id 做数据隔离。
- `brand` 字段作为 PRD 的 `type` 输出（保持兼容，不暴露 `api_key`）。
- 测试接口 `{printer_id}/test`：先实现为“落一条 print_logs 记录 + 返回成功/失败”，不接第三方打印平台也能联调闭环。
验收点：
- PRD 中 3.3.20~3.3.25 的接口均存在且返回字段齐全。

#### E4. 商品规格独立接口：`/api/v1/merchant/products/{product_id}/specs`
改动文件：
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- merchant/product handler（可复用现有 `models.ProductSpec` 与创建/更新商品时的规格写入逻辑）
- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
实现要点：
- GET：返回指定商品的规格列表。
- PUT：整量替换该商品的规格（事务：先删后建）。
- DELETE：删除该商品所有规格。
- 需校验商品归属当前 merchant。
验收点：
- PRD 3.3.11 规格接口可独立联调；不破坏现有商品创建/更新流程。

#### E5. `POST /api/v1/upload/callback`（可选）
改动文件：
- upload handler（[upload.go](file:///e:/other/fz_yyc/server/internal/handlers/upload/upload.go)）
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
实现要点：
- 按 PRD 标注为可选：实现为简单 ACK（200 + code=0），用于兼容未来七牛回调/扩展。
验收点：
- 路由存在且返回统一响应结构。

#### E6. `GET /api/v1/user/merchant/status`（按 phone 查询）
改动文件：
- [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go)
- user handler
- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
实现要点：
- 不走 JWTAuth（PRD 未要求 token）。
- 根据 `phone` 查询 `merchants.contact_phone`，返回 status/status_text/progress 等字段（用 merchants 的 `applyment_status/audit_status/sub_mch_status` 映射）。
验收点：
- 按手机号可查到入驻进度信息（与 PRD 响应字段一致）。

## 执行期验证步骤（实现后必须逐项跑通）

后端：
- `go test ./...`（如有）
- 启动服务后逐项 curl/脚本验证以下关键接口：
  - merchant/register、merchant/print-logs、merchant/subscriptions、merchant/printers*、merchant/products/{id}/specs*
  - user/auth/wechat-login（PRD口径）、auth/user/wechat-login（旧口径兼容）
  - user/addresses*、user/merchant/status
  - sp/auth/login + sp/dashboard（登录前后）
  - callback/wechat + notify/payment（兼容）

小程序：
- 重新编译运行 mp-weixin，验证：
  - 商家入驻页提交成功
  - SP 登录页可进入、可登录、可拉 dashboard
  - 关键列表页请求不再因 token 选择错误而 401

文档：
- 更新 PRD 后，手动核对 PRD“API 对接表”与实现一致（删除 `/api/v1/admin/*`，补齐新增的对外口径说明与字段）。

## 假设与约束

- 当前数据库已按 `full_init.sql` 初始化；新增迁移将以“不破坏现有数据”为原则。
- 统一响应格式沿用 [response.go](file:///e:/other/fz_yyc/server/pkg/response/response.go#L58-L80)。
- 涉及敏感字段（如打印机 `api_key`、服务号 `app_secret`）不在接口响应中返回。

