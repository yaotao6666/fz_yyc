## 总结

目标：全量扫描**服务端已注册路由**、**小程序所有页面与请求调用**、**PRD 中定义的接口**，输出一份“对接差异报告”，用于定位“页面/接口未对接、路径不一致、PRD 与实现不一致”的问题。

本次范围与决策（来自确认）：

- 以 PRD 为准做对照基准，但**本次只输出差异报告，不做修复，不改 PRD、不改前后端代码**。
- 服务商（SP）模块：**只扫描不修复**。

交付物：

- 新增文档：`.trae/documents/接口与页面对接差异报告.md`（差异清单 + 证据链接 + 修复建议但不落地）。

## 当前状态分析（已基于仓库扫描得到的事实）

### 1) 服务端路由现状

- 服务端 HTTP 路由注册集中在 [main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go#L64-L280) 的 `setupRoutes()`，版本前缀为 `/api/v1`（除 `/health`）。
- 发现的高风险点（仅记录，不修复）：
  - `spGroup.Use(JWTAuth())` 覆盖了 `POST /api/v1/sp/auth/login`，按 [JWTAuth](file:///e:/other/fz_yyc/server/internal/middleware/auth.go#L55-L89) 逻辑会导致登录接口无法在未登录状态调用。

### 2) 小程序页面现状

- 小程序路由配置来自 [pages.json](file:///e:/other/fz_yyc/miniprogram/src/pages.json)。
- 代码中存在 `miniprogram/src/pages/sp/**` 服务商页面，但 pages.json 未注册任何 `/pages/sp/...` 路由，属于“页面存在但未纳入路由/入口”的差异点（本次仅报告）。

### 3) 小程序 API 调用现状

- 请求封装： [request.ts](file:///e:/other/fz_yyc/miniprogram/src/utils/request.ts) 与 API 聚合：[api/index.ts](file:///e:/other/fz_yyc/miniprogram/src/api/index.ts)。
- 存在绕过封装的硬编码 URL（例如 `http://localhost:8080/api/v1/...`），本次作为对接风险点记录到报告。

### 4) PRD 与实现已确认的不一致示例

- PRD 中存在 `POST /api/v1/merchant/register`、`GET/POST/PUT/DELETE /api/v1/user/addresses*`、云打印相关 `.../merchant/printers*`、`GET /api/v1/merchant/print-logs` 等接口定义（见 [PRD.md](file:///e:/other/fz_yyc/PRD.md)），但服务端路由注册与 handler 扫描未发现对应实现（本次会在报告中系统化列出全部差异，而非仅举例）。
- PRD 中 `POST /api/v1/user/auth/wechat-login` 与服务端实际 `POST /api/v1/auth/user/wechat-login` 存在路径口径差异（本次会纳入差异报告并给出建议统一方案，但不落地修改）。

## 计划：差异扫描与报告生成

### Step 1：抽取“服务端接口清单”

输入来源：

- [server/cmd/server/main.go](file:///e:/other/fz_yyc/server/cmd/server/main.go#L64-L280)
  抽取规则：
- 以 `setupRoutes()` 内 `Group/GET/POST/PUT/DELETE` 注册为准。
- 输出字段：`方法`、`路径`、`是否 JWTAuth`、`handler(函数名)`、`handler 文件`。
  产物：
- 作为报告的“服务端接口总表”章节，并给出可点击源码引用。

### Step 2：抽取“小程序页面清单（路由维度）”

输入来源：

- [miniprogram/src/pages.json](file:///e:/other/fz_yyc/miniprogram/src/pages.json)
  抽取规则：
- `pages[].path`、tabBar 列表、condition（测试入口）一并整理。
  产物：
- “页面路由总表”章节。
- 额外列出：`src/pages` 目录下存在但 pages.json 未注册的页面（例如 `pages/sp/**`），并标记为“未纳入路由”。

### Step 3：抽取“小程序实际接口调用清单（调用点维度）”

输入来源：

- `miniprogram/src/**`（重点：`src/api/index.ts`、`src/utils/request.ts`、`src/utils/useAuth.ts`、`src/utils/analytics.ts`、各页面 `*.vue`、各 store `*.ts`）
  抽取规则：
- 识别 `get/post/put/del(request)` 的第一个参数（以字符串字面量为主，含模板字符串）。
- 识别 `uni.request`/`uni.uploadFile`/WebSocket URL 等直连调用。
- 输出字段：`方法(若能推断)`、`路径/URL`、`调用文件`、`调用函数/页面`。
  产物：
- “前端调用接口总表”章节。

### Step 4：抽取“PRD 接口清单（文档维度）”

输入来源：

- [PRD.md](file:///e:/other/fz_yyc/PRD.md)
  抽取规则：
- 以形如 `GET /api/...`、`POST /api/...` 的接口定义行进行抓取。
- 归类到模块章节（用户端/商家端/服务商/支付/云打印/地址等）。
  产物：
- “PRD 接口总表”章节。

### Step 5：三方对照与差异分类

对照集合：

- A：服务端路由
- B：前端实际调用
- C：PRD 定义
  输出差异分类（报告必须包含）：
- C 有、A 无：PRD 定义但后端未实现（按模块分组）。
- B 有、A 无：前端调用但后端无路由（前端“悬空接口”）。
- A 有、B 无：后端存在但前端未对接/未使用（可能是遗漏页面或未走到）。
- C 与 A/B 路径不一致：同一能力但接口路径/方法不一致（例如 user wechat-login）。
- 鉴权与调用链风险：例如“登录接口被 JWTAuth 保护”、硬编码 host、baseURL 不一致等。

### Step 6：生成差异报告文档（不修改 PRD/代码）

新增文件：

- `e:/other/fz_yyc/.trae/documents/接口与页面对接差异报告.md`
  报告结构建议：
- 概览（统计：接口总数、差异条数、按模块分布）
- 服务端接口总表
- 页面路由总表
- 前端调用接口总表
- PRD 接口总表
- 差异清单（按上述分类输出，逐条给出：证据链接、影响范围、建议修复方案）

## 假设与边界

- 以当前代码中 `setupRoutes()` 为“服务端接口事实来源”；不推断未注册但可能存在的 handler。
- 前端对接口的“是否对接”以“存在实际调用点/页面引用”为判断依据；仅声明未引用会被标记为“疑似未对接”，需人工确认业务入口。
- 本次不做任何修复与重构，不变更 PRD 内容；报告中的“建议”仅供后续执行阶段使用。

## 验证方式（执行阶段）

- 对 3 个清单（服务端/前端调用/PRD）分别给出总数，并抽样核对若干条目（路径、方法、文件引用正确）。
- 对每一条差异都附带至少一个可点击源码或 PRD 行范围引用，保证可追溯与可复核。

